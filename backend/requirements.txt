# Backend service for the exercise classifier.
# Install with:  pip install -r backend/requirements.txt

fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0

# Needed to load and run the saved artifacts.
joblib>=1.3
scikit-learn>=1.9        # must match the version model.pkl was pickled with
numpy>=1.26
pandas>=2.0

# Used by src/features.py for the FFT, autocorrelation and shape features.
scipy>=1.11
