# Backend service for the exercise classifier.
# Install with:  pip install -r backend/requirements.txt

fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0
python-multipart>=0.0.6  # required by FastAPI to accept file uploads

# Needed to load the saved artifacts and read features.csv.
joblib>=1.3
scikit-learn>=1.9        # must match the version model.pkl was pickled with
numpy>=1.26
pandas>=2.0

# Pulled in by scikit-learn. The service no longer imports src/features.py, so
# nothing here computes features at request time - they come from features.csv.
scipy>=1.11
